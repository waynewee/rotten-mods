
import express from 'express'

const router = express.Router()

// import * as courseController from '../controllers/courseController' 
// import { isLoggedIn } from '../middleware'

// router.get("/", courseController.searchCourses)
// router.get("/:id", courseController.getCourse)
// router.post("/",isLoggedIn, courseController.addCourse)
// router.delete("/:id", isLoggedIn, courseController.deleteCourse)
// router.put("/:id", isLoggedIn, courseController.updateCourse)

export default router;