

import express from 'express'

const router = express.Router()

// import * as plannedModController from '../controllers/plannedModController' 
// import { isLoggedIn } from '../middleware'

// router.get("/user/:userId", plannedModController.getPlannedMods)
// router.delete("/:id", isLoggedIn, plannedModController.deletePlannedMod)
// router.post("/",isLoggedIn, plannedModController.addPlannedMod)
// router.put("/:id",isLoggedIn, plannedModController.updatePlannedMod)

export default router;