import { Input } from "antd";
import "../styles/antd.less";

const SearchBar: React.FC = () => {
  return (
    <div
      style={{
        borderWidth: 2,
        height: 40,
        width: 200,
      }}
    >
      
    </div>
  );
};

export default SearchBar;
